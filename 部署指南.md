# 部署指南

## 服务器信息

| 项目     | 值                        |
| -------- | ------------------------- |
| 地址     | `47.108.188.241`          |
| 用户名   | `root`                    |
| 密钥文件 | `D:\ali_personal_pc.pem`  |
| 部署路径 | `/var/www/math-test`      |
| Web 服务 | nginx（端口 80）          |

## 一键部署脚本

在项目根目录执行以下 PowerShell 脚本：

```powershell
# 配置
$key = "D:\ali_personal_pc.pem"
$server = "root@47.108.188.241"
$remote = "/var/www/math-test/"

# 1. 构建
Write-Host ">>> 构建项目..." -ForegroundColor Cyan
npm run build

# 2. 上传
Write-Host ">>> 上传到服务器..." -ForegroundColor Cyan
scp -i $key dist\index.html "${server}:${remote}index.html"
scp -i $key dist\favicon.svg "${server}:${remote}favicon.svg"
scp -i $key dist\icons.svg "${server}:${remote}icons.svg"
scp -i $key dist\assets\*.css "${server}:${remote}assets/"
scp -i $key dist\assets\*.js "${server}:${remote}assets/"

# 3. 清理旧资源并 reload nginx
Write-Host ">>> 清理旧资源并重启 nginx..." -ForegroundColor Cyan
ssh -i $key $server @"
# 清理旧资源（保留当前新版本）
ls /var/www/math-test/assets/ | grep -v "$(ls dist/assets/ | tr '\n' '|')" | xargs -I {} rm -f /var/www/math-test/assets/{}
nginx -s reload
echo '完成！'
"@
```

## 手动部署步骤

### 1. 本地构建

```bash
npm run build
```

构建产物输出到 `dist/` 目录。

### 2. 上传到服务器

```powershell
# PowerShell
$key = "D:\ali_personal_pc.pem"
$server = "root@47.108.188.241"
$remote = "/var/www/math-test/"

scp -i $key dist\index.html "${server}:${remote}index.html"
scp -i $key dist\favicon.svg "${server}:${remote}favicon.svg"
scp -i $key dist\icons.svg "${server}:${remote}icons.svg"
scp -i $key dist\assets\*.css "${server}:${remote}assets/"
scp -i $key dist\assets\*.js "${server}:${remote}assets/"
```

### 3. 重启 nginx

```powershell
ssh -i D:\ali_personal_pc.pem root@47.108.188.241 "nginx -s reload"
```

## 验证

浏览器打开 http://47.108.188.241 ，或命令行检查 HTTP 状态：

```powershell
curl.exe -s -o /dev/null -w "HTTP Status: %{http_code}\n" http://47.108.188.241
```

## nginx 配置

服务器上的 nginx 站点配置位于 `/etc/nginx/sites-available/math-test`：

```nginx
server {
    listen 80;
    server_name 47.108.188.241;
    root /var/www/math-test;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control public;
    }
}
```

> **注意：** 部署后浏览器可能需要 **Ctrl+F5 强制刷新** 以加载最新资源。

## 快捷部署

在项目根目录的终端中直接输入：

```
/deploy
```

Claude 会自动执行构建、上传、清理、重启的完整流程。
